HEAD
{
  "name": "hookahplus",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run generate",
    "dev": "vite"
  },
  "dependencies": {},
  "devDependencies": {}
}

{
  "name": "hookahplus",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run generate",
    "dev": "vite"
  },
  "dependencies": {},
  "devDependencies": {}
}
3322489f (Fixed malformed package.json for Netlify build)
