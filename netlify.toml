[build]

  publish = "build"
  command = "npm run build"

  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
cd2ad59e (� Fix App Router conflict – removed pages/, confirmed working next build)
