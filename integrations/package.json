{
  "name": "hookahplus-integrations",
  "version": "1.0.0",
  "description": "POS integrations for Hookah+ MVP",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.ts"],
    "collectCoverageFrom": [
      "**/*.ts",
      "!**/__tests__/**"
    ]
  }
}
